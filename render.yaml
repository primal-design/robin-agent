services:
  - type: web
    name: robin-agent
    runtime: node
    rootDir: apps/api
    buildCommand: cd ../web && npm install && npm run build && cd ../api && npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
